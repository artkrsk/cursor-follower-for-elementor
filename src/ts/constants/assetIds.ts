/** ids stamped on the gate-injected tags — WP-convention-shaped
    ({handle}-js / {handle}-css, what wp_enqueue_* would have produced) so they
    read as enqueued assets in DevTools and stay targetable by site owners'
    exclusion rules. Also the gate's double-injection guard key. */
export const GATE_CSS_ID = 'arts-cursor-follower-for-elementor-css'
export const GATE_JS_ID = 'arts-cursor-follower-for-elementor-js'
