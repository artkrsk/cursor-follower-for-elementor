/** What the engine treats as interactive without a payload. */
export const INTERACTIVE_SELECTOR = 'a, button, [role="button"], .has-cursor-highlight'
/** Author opt-outs. */
export const NO_TARGET_SELECTOR = '.no-cursor-target'
export const NO_HIGHLIGHT_SELECTOR = '.no-cursor-highlight'
export const DISABLED_SELECTOR = '[disabled], [aria-disabled="true"]'
